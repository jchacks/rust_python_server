[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"


[project]
name = "example"
description = "example model"
version = "0.1.0"
dependencies = [ "locust","flask","scikit-learn","xgboost" ]

[tool.setuptools]
packages = ["example"]
